#include "main.h"

/**
 * _abs - computes the absolute avlue of a number
 *@n: integer to calculate its absolute value
 * Return: Absolute value of n
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
